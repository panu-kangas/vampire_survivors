#pragma once

enum eButtonType
{
	PLUS,
	MINUS,
	CONTINUE
};

enum eFloorColor
{
	NORMAL,
	RED,
	GREEN
};

enum eDirection
{
    LEFT,
    RIGHT,
    UP,
    DOWN
};

enum eAttackType
{
	LANCE,
	HOLY_PULSE
};

enum VampireTypes
{
	WHITE_VAMPIRE,
	RED_VAMPIRE,
	GREEN_VAMPIRE,
	BOSS_VAMPIRE
};